name: Compliance Placeholder

on:
  workflow_dispatch:

jobs:
  compliance-note:
    runs-on: ubuntu-latest
    steps:
      - name: Compliance mode
        run: echo "Automated playlist generation is disabled for compliance reasons."
